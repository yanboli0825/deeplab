defaults:
  - override /model: cpath/tabular_classification
  - override /datamodule: cpath/tabular_manifest
  - override /logger: mlflow
  - override /trainer: default

experiment_name: tabular_baseline
run_name: baseline
monitor: val/loss
test_after_train: true
