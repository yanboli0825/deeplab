items:
  - _target_: lightning.pytorch.loggers.WandbLogger
    project: ${project_name}
    name: ${run_name}
    save_dir: ${paths.output_dir}
    offline: false
    log_model: false
    group: default
